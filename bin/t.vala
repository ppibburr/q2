public class Bar {
  public Type ary {;
    int[] a;
    a = [1, 2, 3];
  }
}
public class Foo : Bar {
  public  Foo(a, b) {;
    int[] a;
    var ba = Bar.ary();
    for (var i_n=0; i_n < ba.length; i_n++) {;
      var i = ba[i_n];
      print(i.to_string()+"\n");
    }
    a = [1, 2, 3];
    foreach ( var q in a )  { print(q.to_string()+"\n"); }
    var x = (c()) ? (1) : (2);
    var d = ((x > 1)) ? (5) : (((x == 2)) ? (6) : (7));
    bar(1, 2);
  }
  
  public Type bar(a, b) {;
    print("%d"+"\n",(a + b));
  }
  
  static void main() { new Foo(1,2); }
}
